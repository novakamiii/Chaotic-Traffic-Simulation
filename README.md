# Site

**http://chaotictrafficsim.freedev.app/**

# Running the project locally

1. Clone this repo.
2. Go to the project root.
3. On the terminal, run `npm i && npm update` to install and update packages.
4. Run `chmod +X start.sh` to make it as an executable.
5. Serve the project via `./start.sh`
